import 'package:flutter/material.dart';

class InterestEvent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    //todo:built Interest Event Screen

    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text(
          "Interest Event",
          style: TextStyle(color: Colors.white),
        ),
      ),
    );
  }
}
