import 'package:flutter/material.dart';

class EventHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    //todo: built Event History Screen
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text(
          "Event History",
          style: TextStyle(color: Colors.white),
        ),
      ),
    );
  }
}
