import 'package:flutter/material.dart';

class HelpScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    //todo:built Help Screen
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text(
          " Help Screen",
          style: TextStyle(color: Colors.white),
        ),
      ),
    );
  }
}
